:properties:
:ID: 20251223T085835
:end:
#+title:      KDOC 519: Goのmapではキーの値が同じでも型情報が違えば別のものとして扱われる
#+date:       [2025-12-23 Tue 08:58]
#+filetags:   :permanent:
#+identifier: 20251223T085835

* この文書のステータス
- 作成
  - [X] 2026-02-22 貴島
- レビュー
  - [X] 2026-02-25 貴島

* 概要

型が違えば同じ値であっても違うものとして扱われる。当然にも見えるが、直感的ではない状況に見えることもある。

#+begin_src go
  package main

  import "fmt"

  type a string
  type b string

  func main() {
  	m := map[any]any{}
  	m[a("k")] = "v"
  	m[b("k")] = "v"

  	fmt.Printf("%#v", m)
  }
#+end_src

#+RESULTS:
#+begin_src
map[interface {}]interface {}{"k":"v", "k":"v"}
#+end_src

出力した表示上は型が表示されていないので、キーがかぶって登録されているように見える。が、型は違うので扱いは異なる。

* 関連

なし。
